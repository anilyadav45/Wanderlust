<%- layout("/layouts/boilerplate") %>

  <body>
    <div class="show-container">
      <div class="listing-container">
        <% if (listing.image && listing.image.url) { %>
          <img src="<%= listing.image.url %>" alt="<%= listing.title %>" />
          <% } %>

            <div class="listing-title">
              <%= listing.title %>
            </div>
            <div class="listing-description">
              <%= listing.description %>
            </div>

            <div class="listing-price">
              &#8377; <%= listing.price ? listing.price.toLocaleString("en-IN") : "0" %>
            </div>
            <div class="listing-location">
              <%= listing.location %>, <%= listing.country %>
            </div>

            <div class="listing-actions">
              <a href="/listings/<%= listing._id %>/edit">Edit this Listing</a>
              <form method="POST" action="/listings/<%=listing._id%>?_method=DELETE">
                <button>Delete this listing</button>
              </form>
            </div>
            <br>
            <hr>
            <div class="review mt-3 mb-3">
              <h3>Leave a Review</h3>
              <form action="/listings/<%= listing._id %>/reviews" method="POST" novalidate class="needs-validation">
                <div class="review-box mt-3 mb-3">
                  <label for="rating">rating</label>
                  <input class="form-label form-range" type="range" name="review[rating]" id="rating" min="1" max="5" required>
                </div>
                <div class="comment-box mt-3 mb-3">
                  <label for="comment">comment</label>
                  <textarea class="form-label form-control" name="review[comment]" id="comment" cols="30" rows="5" required></textarea>
                  <div class="invalid-feedback">Please leave  a comment for feedback!</div>
                </div>
                <button class="btn btn-outline-dark">Submit</button>
              </form>
            </div>

      </div>

    </div>
  </body>